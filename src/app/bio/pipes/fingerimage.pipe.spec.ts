import { FingerimagePipe } from './fingerimage.pipe';

describe('FingerimagePipe', () => {
  it('create an instance', () => {
    const pipe = new FingerimagePipe();
    expect(pipe).toBeTruthy();
  });
});
